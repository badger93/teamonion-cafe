
const initState = {};


const userReducer = (state = initState, action) => {
  switch (action.type) {
    // case :
    //   return { ...state };

    default:
      return state;
  }
};

export default userReducer;
